import React from "react";
import CreateUserForm from "./CreateUserForm"; // Adjust the path as needed

const CreateUserPage = () => {
  return (
    <div>
      <CreateUserForm />
    </div>
  );
};

export default CreateUserPage;
